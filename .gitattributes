# Shell scripts must keep LF endings — they are run by git-bundled bash even on
# Windows (e.g. scripts/git-credential-env.sh as a git credential helper). A
# CRLF checkout would break the credential helper.
*.sh text eol=lf
