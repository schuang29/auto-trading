# Daily Log — 2026-05-13 (Wednesday)

## RELIABILITY GAP — NO ROUTINE RAN

**This is an acknowledged-gap marker, written 2026-05-17 during incident
remediation. It is not a routine log.**

No routine ran on 2026-05-13. There is no pre-market, market-open, midday, or
EOD output for this trading day. No `portfolio_daily.csv`, `positions_daily.csv`,
or `benchmarks_daily.csv` row exists for this date, and no decision files were
produced.

**Root cause:** all five `AutoTrading-*` scheduled tasks were registered with
the `New-ScheduledTaskSettingsSet` default `DisallowStartIfOnBatteries=$true`.
The laptop was on battery at the scheduled times on 2026-05-13, 05-14, and
05-15, so every task silently declined to start. Windows does not count
battery-blocked starts in `NumberOfMissedRuns`, so the scheduler reported
"healthy" while the bot was dark. Full analysis: `memory/weekly/2026-W20.md`
and `memory/health/2026-W20-reliability-incident.md`.

**No data is fabricated for this day.** Per hard rule #5 (append-only audit
trail) and rule #6 (no invented rationale), the gap is recorded as a gap. It
is NOT reconstructed: Alpaca paper history cannot reliably rebuild per-position
snapshots for an arbitrary past date, and inventing equity/price rows would
corrupt the audit trail. The day is simply, honestly, missing.

**Position/portfolio state during this window is unknown.** The last verified
state before the gap is the 2026-05-12 EOD log; the next verified state is the
2026-05-17 catch-up run.

This marker exists so the dead-man's-switch (`scripts/heartbeat.py`) treats
this day as a documented, acknowledged outage rather than re-alerting on it
indefinitely.
